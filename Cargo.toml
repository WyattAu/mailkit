[package]
name = "mailkit"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Email delivery for Rust — SMTP (lettre) and Resend providers with template rendering, queue, and audit logging"
license = "MIT OR Apache-2.0"
repository = "https://github.com/WyattAu/mailkit"
keywords = ["email", "smtp", "resend", "template", "queue"]
categories = ["email", "asynchronous"]

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
tokio = "1"
reqwest = { version = "0.12", features = ["json"], optional = true }
lettre = { version = "0.11", optional = true }
tera = { version = "1", optional = true }
chrono = { version = "0.4", features = ["serde"] }
serde_json = "1"

[features]
default = ["resend"]
resend = ["dep:reqwest"]
smtp = ["dep:lettre"]
templates = ["dep:tera"]

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
